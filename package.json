{
  "name": "sneakerbot-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "contentful": "^11.7.15"
  }
}
